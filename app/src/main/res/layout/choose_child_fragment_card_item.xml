<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/card_view_choose_child"
    android:layout_width="match_parent"
    android:layout_height="@dimen/choose_child_card_item_height"
    android:layout_gravity="center"
    android:layout_marginBottom="@dimen/md_keylines"
    android:layout_marginLeft="@dimen/md_keylines"
    android:layout_marginRight="@dimen/md_keylines"
    android:foreground="?attr/selectableItemBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <ImageButton
            android:id="@+id/imageButton"
            android:layout_width="match_parent"
            android:padding="0dp"
            android:layout_height="@dimen/choose_child_image_button_height"
            android:onClick="newActivityGoToChildMainPanelActivity"
            android:src="@drawable/elvispresley"
            android:scaleType="centerCrop" />

        <TextView
            android:id="@+id/childName"
            android:layout_width="match_parent"
            android:layout_height="@dimen/choose_child_text_view_height"
            android:layout_below="@+id/imageButton"
            android:layout_marginLeft="@dimen/md_keylines"
            android:textSize="30sp"
            android:textColor="@color/primaryText"
            android:textAppearance="@style/TextAppearance.AppCompat.Title" />

    </LinearLayout>

</android.support.v7.widget.CardView>